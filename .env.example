# Base API host (no trailing slash). RTK Query appends /v1
NEXT_PUBLIC_API_URL=http://localhost:3050
# Optional Socket.IO host (defaults to NEXT_PUBLIC_API_URL)
NEXT_PUBLIC_SOCKET_URL=http://localhost:3050
